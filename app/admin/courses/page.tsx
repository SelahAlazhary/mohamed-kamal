import { redirect } from "next/navigation";

/**
 * ‎/admin/courses — بابٌ كان مسدوداً.
 * ------------------------------------------------------------------
 * دروسُ كورسٍ بعينه على `‎/admin/courses/<id>`، فمن حذف المعرّفَ من
 * العنوان — أو كتبه من ذاكرته — وقع على ٤٠٤ لا يقول له أين يذهب.
 * والقائمةُ نفسُها على `‎/admin/subjects`، فيُحوَّل إليها.
 *
 * وتحويلٌ دائمٌ لا صفحةُ خطأ: العنوانُ معقولٌ ومقصدُه واضح، وردُّ «غير
 * موجود» على عنوانٍ معقولٍ عيبٌ في الموقع لا في كاتبه.
 */
export default function CoursesIndex() {
  redirect("/admin/subjects");
}
